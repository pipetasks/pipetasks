import styled from 'styled-components';
import { ContainerColumn } from '../../assets/containers';

export const DashboardContainer = styled(ContainerColumn)`
  @media (max-width: 1120px) {
    padding: 0;
  }
`;
