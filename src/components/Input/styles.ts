import styled from 'styled-components';

export const ContainerInput = styled.label`
  width: 100%;
  input {
    border: 0 none;

    color: var(--black);

    width: 100%;
  }

  input::placeholder {
    color: var(--black-800);
  }
`;
